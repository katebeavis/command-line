The 'cp' command is also known as the 'copy' command, it copies both files and directories.
cp iamkate iamjames - This will copy 'iamkate' and rename it 'iamjames' within the current directory.
cp iamkate ~/newfolder - This will copy 'iamkate' to the directory newfolder.
cp -r unixstuff ~/newfolder - This will copy the directory unixstuff and its contents to the directory newfolder.
